#include <iostream>
#include <vector>
#include <cstring>
#include <thread>
#include <chrono>

// get car name
std::string getCarName(uint8_t modelId);

// get track name
std::string getTrackName(uint8_t trackId);